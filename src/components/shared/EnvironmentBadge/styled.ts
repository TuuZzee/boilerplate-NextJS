import { Badge } from 'reactstrap';
import styled from 'styled-components';

export const EnvBadgeBlock = styled.div`
  position: fixed;
  left: 10px;
  top: 90%;
`;

export const EnvBadgeMessage = styled(Badge)`
  width: 50px;
`;
